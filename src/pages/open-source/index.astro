---
import Base from '../../layouts/Base.astro';
import PostRow from '../../components/PostRow.astro';
import Empty from '../../components/Empty.astro';
import { site } from '../../site.config';
import { published, url } from '../../lib/utils';
const sec = site.sections.openSource;
if (!sec.enabled) return Astro.redirect(url('/'));
const posts = await published('open-source');
---
<Base title={sec.title} description={sec.blurb}>
  <header class="page-head"><h1>{sec.title}</h1><p>{sec.blurb}</p></header>
  {posts.length
    ? posts.map((p) => <PostRow href={`/open-source/${p.id}/`} title={p.data.title} summary={p.data.summary} date={p.data.date} body={p.body} extra={p.data.project} />)
    : <Empty text="First contributions coming soon." />}
</Base>
